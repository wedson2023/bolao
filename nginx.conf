worker_processes 1;

events {
	worker_connections 1024;
}

http {
	include mime.types;
	default_type application/octet-stream;
	sendfile on;
	keepalive_timeout 65;
	gzip on;

	server {
		listen 80;
		server_name localhost;
		access_log logs/access.log;

		location {
			root /var/www/html/bolao;
			expires max;
		}

		location / {
			proxy_pass http://165.227.65.14:3000;
		}
	}
}